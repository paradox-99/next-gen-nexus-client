import { useQuery } from "@tanstack/react-query";

const useSupscription = () => {

    const { data, error, isLoading } = useQuery({
        
    })

    return (
        <div>
            
        </div>
    );
};

export default useSupscription;